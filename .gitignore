# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Dropbox
.dropbox
.dropbox.attr
*conflicted copy*
*Conflicted Copy*

# Editors
.vscode/
.idea/
*.swp

# Vercel
.vercel

# Node (only if you ever add tooling)
node_modules/

# Local infrastructure notes — keep out of the public repo
DNS-BACKUP-*.txt

# Draft correspondence about correcting other people's published bios.
# Private working material; the repo is public.
BIO-CORRECTIONS.md

# Sandbox/FUSE leftovers from editing over a mounted filesystem.
# Deleted-but-still-open file handles; never real content.
.fuse_hidden*

# Local captures of other people's articles, kept as a personal backup in case
# a source goes offline. Never published — the repo is public and these are
# third-party copyrighted works. The public durability fix is the Wayback link
# beside each item on the live site. See press-archive/README.md.
press-archive/
